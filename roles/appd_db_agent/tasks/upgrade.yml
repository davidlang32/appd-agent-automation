---
# STARTER STUB — implement version swap, then re-apply config.
- name: Upgrade then reconfigure (DB agent)
  ansible.builtin.include_tasks: install.yml
- ansible.builtin.include_tasks: configure.yml
